import 'package:flutter/material.dart';

class QuadTreePage extends StatelessWidget {
  static String routePath = '/quad-tree';
  static String routeName = 'Quad Tree';

  const QuadTreePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
